<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <title>Sample Application</title>
    <link href='//fonts.googleapis.com/css?family=Lato:300' rel='stylesheet' type='text/css'>
    <style>
        body {
            margin: 50px 0 0 0;
            padding: 0;
            width: 100%;
            font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
            text-align: left;
            color: #aaa;
            font-size: 18px;
        }
        h1 {
            color: black;
            letter-spacing: -3px;
            font-family: 'Lato', sans-serif;
            font-size: 40px;
            font-weight: 200;
            margin-bottom: 0;
        }
    </style>
</head>
<body>
<h1>Demo Application</h1>
<div>All Active Posts</div>
{% if posts is empty %}
    <p>No Posts Found</p>
{% else %}
<table border="1">
    <thead style="font-style: italic">
    <tr>
        <th>Title</th>
        <th>Description</th>
        <th>Date Created</th>
        <th>Author</th>
        <th>More</th>
    </tr>
    </thead>
    {% for post in posts %}
    <tr>
        <td>{{post.title}}</td>
        <td>{{post.shortText}}</td>
        <td>{{post.created|date('F j, Y')}}</td>
        <td>{{post.author}}</td>
        <td><a href="/content/detail/{{post.id}}">Read More</a></td>
    </tr>
    {% endfor %}
</table>
{% endif %}
</body>
</html>